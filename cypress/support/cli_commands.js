// not including git clone test because of ssh key problems

// Cypress.Commands.add('clone_via_SSH', project_infos => {
//     const domain = Cypress.config('baseUrl').replace('http://', '')
  
//     cy.exec(`cd cypress/downloads`);
//     cy.exec(`git clone git@${domain}:${Cypress.env('user_name')}/${project_infos.name}.git`);

  